{
  "name": "gerador-de-provas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node tests/run-tests.js",
    "serve": "python -m http.server 8000 --bind 127.0.0.1",
    "start": "npm run serve"
  }
}